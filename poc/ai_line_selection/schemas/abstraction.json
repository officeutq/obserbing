{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "obserbing://schemas/abstraction/abstraction-only-v1",
  "type": "object",
  "required": ["schema_version", "abstraction"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "type": "string", "enum": ["abstraction-only-v1"] },
    "abstraction": { "type": "string", "minLength": 2, "maxLength": 60 }
  }
}
