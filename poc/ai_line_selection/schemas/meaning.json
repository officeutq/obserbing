{
  "type": "object",
  "required": ["schema_version", "themes", "structure", "abstraction"],
  "additionalProperties": false,
  "properties": {
    "schema_version": { "type": "string", "enum": ["draft-1"] },
    "themes": {
      "type": "array",
      "items": { "type": "string" }
    },
    "structure": { "type": "string" },
    "abstraction": { "type": "string" }
  }
}
